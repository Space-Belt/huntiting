import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const HuntListScreen = (props: Props) => {
  return (
    <View>
      <Text>HuntListScreen</Text>
    </View>
  );
};

export default HuntListScreen;

const styles = StyleSheet.create({});
